# 씬 4 — 오피스 섹터 (office-sector)

```
web/?scene=office-sector        # 활성 씬 — 아무것도 안 붙이면 여기가 뜬다
```

지하 500m 연구시설의 사무 구역 1층. **이 저장소의 첫 실내 씬**이다.

- **왜 이렇게 생겼나**는 [facility.md](facility.md) 에 있다. 치수를 고치기 전에 거기부터.
- 여기는 **무엇이 어떻게 돌아가고, 무엇이 남았나** 다.

---

## 1. 지금 (2026-08-04)

```
삼각형 126,276 · 텍스처 33장 38MB · 재질 37 · 드로우그룹 40 · 셰이더 6 · 빌드 4.0초
칸 15 · 벽 40구간 413.6m · 문 16 · 등 135 · 소품 294
런타임 광원 2 (환경광 + 반구광) — 40개는 정점에 구웠다
```

| 검사 | 결과 |
|---|---|
| 평면 겹침 | 0 — 48x34 를 **100.0% 정확히** 채운다 |
| 도달 | **15/15** — 플라자에서 모든 방에 걸어갈 수 있다 |
| 좁아서 문 못 낸 곳 | 0 |
| 캄캄한 방 | 0 |
| 조도 위계 | 밝기 폭 **4.18배** (플라자 → 서버룸) |
| 감사 · 배치 · 조명 결함 | 0 · 0 · 0 |
| 회귀 | of_ 5뷰 일치 (다른 씬 6뷰도 픽셀 동일) |

소품 내역: 스프링클러 103 · 랙 45 · 책상 34 · 배전반 32 · 선반 24 · 유도등 14 ·
급기구 12 · 식탁 9 · 덕트 6 · 자판기 4 · 벤치 4 · 배관 4 · 냉방기 2 · 안내기둥 1.

---

## 2. 생성기의 구조 — 어디를 고치면 무엇이 바뀌나

```
layout.js    평면의 유일한 출처. 칸 · 치수 · 벽 규칙 · 평면 검사
  ├ shell.js     바닥 · 벽 · 천장 · 암반
  ├ props.js     소품 (용도별)
  ├ lights.js    등과 광원의 자리 (좌표만)
  │   └ bake.js      그 자리에서 빛을 정점에 굽는다
  ├ materials.js 재질 묶음  ← textures.js 가 굽는 레시피를 쓴다
  └ check.js     실내 전용 검사 (캄캄한 방 · 머리 닿음 · 조도 위계)
index.js     위를 순서대로 부르고, 결함을 배치 검사에 넘긴다
```

### 2.1 layout.js — 여기가 평면이다

| 내보내는 것 | 무엇 |
|---|---|
| `H` `W` `FLOOR` | 치수. facility.md 2장의 표가 그대로 온다 |
| `CELLS` | 축에 나란한 사각형 15개. `kind`(벽·조명 규칙) 와 `use`(소품) 가 따로다 |
| `PAIR` | 두 `kind` 사이에 무엇이 서나 — open/door/wide/glass/solid |
| `wallRuns()` | 층 전체의 **벽** 구간. 한 경계를 두 번 안 짓는다 |
| `openness()` | 칸 사이가 얼마나 트였나 (**가공 전** 규칙). 도달 검사와 빛 굽기가 같이 쓴다 |
| `interiorWalls(id)` `onWall(w,t,d)` | 소품이 붙을 면. 문 자리는 이미 빠져 있다 |
| `checkPlan()` `checkReach()` | 겹침 · 층 채움 · 갇힌 방 |

**새 섹터를 만들려면** `CELLS` 와 `PAIR`, 그리고 `props.BY_USE` 의 용도 몇 개만
고치면 된다는 것이 이 판의 가설이다 (6장, 아직 미검증).

### 2.2 벽에 구멍은 불리언으로 안 뚫는다

칸의 경계를 훑어 **이웃이 같은 구간으로 쪼개고**, 규칙표를 보고 열지 말지
정한다. 도시의 `streetFaces` 와 같은 자리다.

```
edgeSpans()  경계를 이웃이 바뀌는 지점마다 자른다 -> 같은 이웃끼리 합친다
wallRuns()   층 전체의 벽 구간
openingsOf() 그 구간에 뚫릴 문
```

합치기를 안 했더니 68구간이 나왔다 — 마주보지도 않는 칸의 경계값이 벽을
셋으로 쪼개 **18m 벽 하나에 문이 세 개** 섰다. 합친 뒤 40구간.

**소핏** — 플라자(5.6)를 고리 복도(2.7)가 감싼다. 바닥에서는 트여 있지만 그
사이에 2.9m 수직면이 생긴다. 안 세우면 복도 천장 위 설비가 들여다보인다.

### 2.3 조명 — 좌표는 하나, 쓰는 곳은 셋

```
lights.lightPlan()   등과 광원의 자리를 낸다 (지오메트리도 THREE 객체도 안 만듦)
  ├ createFixtures()   보이는 것 — 발광판 + 반사갓
  ├ bake.bakeVertexLight()   밝히는 것 — 정점 컬러로 굽는다   ← 기본
  └ createRealLights()       밝히는 것 — 실시간 PointLight    ← bake.BAKED = false
```

두 조명 경로가 **같은 계획과 같은 지오메트리**를 쓰므로, A/B 화면 차이는
오로지 조명 차이다. 굽기 근사 셋:

| | |
|---|---|
| 등은 **아래를 보는 면** | 반지름 0.7m 구로 본다. 점으로 두면 가까이서 발산해 정점 하나만 튄다. 배광에 바닥(0.32)을 깔아 등과 같은 높이의 덕트도 받게 했다 |
| 칸 사이는 **틈의 높이**로 막는다 | 플라자의 등이 고리 복도 바닥을 못 비추는 것이 이 한 줄이다 — 소핏이 막는다. 가로 위치는 트인 비율로 뭉갠다 |
| 바운스는 **바닥에서 온다** | 아래를 보는 면이 제일 많이 받고 높이가 오를수록 준다 |

**정점 간격이 곧 라이트맵 해상도다.** `MeshBuilder({span})` 으로 상자를 쪼갠다
— 벽 1.2m · 바닥 1.5m · 천장 3.0m. 천장이 제일 성긴 이유는 등이 아래를 보므로
천장이 직사광을 0 으로 받기 때문이다.

레벨은 눈이 아니라 분포로 맞춘다 — 상위 0.5%(p99.5)가 1.0 바로 아래(0.81)에
오도록 `GAIN` 을 잡는다. 그 위는 잘리고, 그 아래로 남기면 계조를 버린다.

### 2.4 무엇을 재나

```
checkPlan()      칸이 겹치나 · 층을 채우나 · 너무 작은 칸이 있나 -> 빌드를 던진다
checkReach()     문 그래프를 BFS. 갇힌 방을 잡는다
checkInterior()  캄캄한 방 · 머리 닿는 통로 · 천장 격자 어긋남
checkLighting()  플라자가 제일 밝나 · 닫힌 복도가 방보다 어둡나 · 폭이 3배는 되나
```

`layout.PAIR` 는 **스스로를 검사한다** — 키가 알파벳순인가, 쓰이는 종류 조합이
다 있는가. 로드하다 두 번 터졌고 둘 다 화면을 보기 전에 잡혔다.

**조도에 반사율을 곱해 잰다.** 구운 값은 "닿은 빛" 이고 보이는 것은 거기에
재질이 한 번 더 곱해진 값이다 — 플라자가 빛을 2배 받고도 화면에서 복도에
밀렸던 것이 이 차이였다 (`lessons.md` 3.10).

---

## 3. 이 씬이 세운 것

### 계측을 형태보다 먼저

캐릭터 씬에서 **"30뷰 일치, 경고 0" 을 네 번 보고하는 동안 얼굴이 파여
있었다** (`lessons.md` 3.8). 실내는 다행히 전부 숫자로 잡히므로, 이번엔
형태를 짓기 전에 검사부터 만들었다. 그 판단은 값을 했다 — `PAIR` 자기검사 ·
도달 검사 · 소품 수 셋이 각각 화면에 나오기 전에 결함을 잡았다.

**다만 계측이 못 보는 축은 그대로 남았다.** 화면·재질·"그 물건으로 읽히나"
는 숫자가 아니라 사람이 봐야 한다. 이 씬에서 실제로 그렇게 새어 나간 것이
아래 4장 절반이다.

### 인상을 바꾼 것 셋

| | |
|---|---|
| **굽도리** | 바닥에서 1.1m 색이 다른 띠. 위아래가 같으면 복도가 관처럼 보인다. 한 줄로 제일 크게 바뀌었다 |
| **원기둥** | 배관을 상자로 냈더니 천장에 각목이 걸린 것 같았다. **실루엣이 그 물건을 정한다** — 의자 다리·전선관·냉매 배관까지 같은 이야기다 |
| **문 너머가 보인다** | 플라자에서 카페테리아의 식탁과 창고의 선반이 보인다. 실내의 깊이는 여기서 나온다 |

---

## 4. 걸린 것과 그 원인

### 4.1 평면과 조명

| | 증상 | 원인 |
|---|---|---|
| 1 | 방이 통째로 캄캄 | 점광원 세기를 **방향광 단위**로 썼다 (2.6). 점광원은 거리 제곱으로 죽어 3m 아래서 1/9 다 → 13~60 |
| 2 | 천장만 새카맣다 | **타공에 높이차 0.3.** 1~2mm 구멍인데 노말이 눕어 바로 아래 형광등을 놓쳤다 |
| 3 | 고쳐도 여전히 검다 | **진짜 원인.** 등은 4.8m 마다인데 광원은 칸당 하나(`면적/60`). 22.8m 복도가 하나를 받아 10m 떨어진 천장이 1/63 로 죽었다 |
| 4 | 문 하나에 유도등 넷 | 벽 **조각**마다 달았다. 조각은 "벽이 남은 자리" 지 "문 자리" 가 아니다 |
| 5 | 플라자에 벤치가 0개 | 사방이 트여 바닥 높이에 벽이 없다. `interiorWalls('plaza')` 가 빈 배열이었고, **개수를 안 셌으면 못 봤다** |
| 6 | 밋밋하다 | 방출면 코사인의 **부호를 뒤집어** 바닥이 직사광 0, 천장만 받았다. 칸별 `min = max = mean` 이 잡았다 |
| 7 | 전체가 어둡다 | 레벨을 눈으로 맞추려 했다. p99.5 가 0.46 이라 계조 절반을 버리고 있었다 → 1.95배 |
| 8 | **플라자가 복도보다 어둡다** | 조도만 쟀다. 플라자는 빛을 2배 받지만 바닥 반사율이 0.25, 복도는 0.52 였다 |

3번과 8번이 이 판의 교훈이다. 3번은 **문서에 "패널과 광원을 짝으로 둔다" 고
써 놓고 코드에서 깬 것**이고, 8번은 **재는 축이 틀렸던 것**이다.

### 4.2 재질이 형태를 먹은 것 — 세 번 반복

세 번 다 같은 병이다. 형태를 고쳐 놓고 화면이 안 변해서 조명을 의심했는데,
구운 값을 재 보면 멀쩡했고 범인은 알베도였다.

| 물건 | 구운 값 | 범인 |
|---|---|---|
| 덕트 | 0.26 (정상) | `metalness 0.45` — 금속은 확산 알베도가 없다 |
| 책상 서랍장 | 정상 | `steelDark` 알베도 0.13 |
| 서버 유닛 U 칸 | 0.25 (정상) | `bezel` 색 `0x40454d` → 알베도 0.056 |

**형태를 나눴는데 안 보이면 조명이 아니라 알베도부터 본다** (`lessons.md` 3.12).

덕트에서는 그 전에 조명을 두 번 고쳤다 — 광원 높이(천장 밑 0.35m 안이 통째로
그늘이었다)와 배광(순수 코사인은 자기 높이를 못 비춘다). **둘 다 맞는
수정이지만 화면은 거의 안 변했고, 그동안 값을 한 번도 안 쟀다.**

### 4.3 좌표 규약을 둘로 둔 것

창고 상자 백여 개가 **옆방 카페테리아 한가운데 공중에** 떠 있었다.

```js
const [px, pz] = wallAt(w, uu, 0.32, 0);   // wallAt 은 [x, y, z] 를 돌려준다
```

`pz` 에 y(=0) 가 들어갔다. 같은 파일의 `onWall` 은 `[x, z]` 를 돌려주므로
규약이 둘이었고 새 것을 옛 습관으로 받았다. 감사도 배치 검사도 통과했다 —
지오메트리는 멀쩡히 있고 겹치지도 않았다.

### 4.4 "디테일" 을 이름만 붙이고 넘어간 것

`status.md` 에 "소품 마감 **완료**" 라고 적었을 때, 컴퓨터는 화면이 몸통
뒷면에 붙어 있었고(34대 전부 사람 반대쪽) 자판기는 색칠한 상자였다.
목록에서 지웠다고 그 일이 된 것이 아니었다.

**실루엣으로 읽히는 물건과 디테일이 있어야만 읽히는 물건은 예산이 다르다.**
의자·책상은 다리 넷과 상판이면 되지만, 배전반·서버랙·자판기는 아니다.
3단계에 들어가면 물건마다 아래 같은 목록을 먼저 적고 그것에 대고 확인한다
(`lessons.md` 3.13 · 3.13.1).

| 물건 | 그 물건으로 만드는 조각 |
|---|---|
| 브라운관 | 사람 쪽을 보는 화면 · 두꺼운 베젤 · 뒤로 좁아지는 몸통 · 받침 |
| 자판 | 줄이 보여야 한다 (키를 하나씩 내면 책상 34개에 2천 개) |
| 골판지 상자 | 크라프트 색 · 뚜껑 접힌 자리와 테이프 · 라벨 + 크기·각도·개수의 변주 |
| 배전반 | 여닫는 문 · 통풍 루버 · **노란 경고 딱지** · 천장으로 나가는 전선관 |
| 서버 랙 | U 단위 유닛(빈 칸·블랭크 포함) · **잠기는 망문** · 프레임 기둥 · 랙 번호 |
| 냉방기 | 가로 루버가 빽빽한 흡입 그릴 · 조작반 · 냉매 배관 |
| 자판기 | **진열창 안의 상품 줄** · 간판 · 선택 버튼 · 투입구 · 안으로 들어간 배출구 |

랙 열은 **콜드/핫 아일**로 번갈아 돌렸다. 전부 같은 방향을 보면 앞 열의
배기를 뒤 열이 빨아들인다.

---

## 5. 열려 있는 것

### 5.1 코드리뷰에서 나온 결함 (2026-08-04)

근접 렌더로 실측해 확인한 것들. **1~3 은 "완료" 라고 쓴 작업 안에 있던 결함**이다.

| | 무엇 | 어디 |
|---|---|---|
| **1** | **자판기 진열창의 상품이 안 보인다.** 본체가 `v 0~0.8` 을 채운 불투명 상자인데 상품·선반이 `v 0.705~0.755` 즉 본체 속이다. 벽처럼 **창 둘레를 틀로 쪼개** 지어야 한다 | `props.vendor` |
| **2** | 표시창·조작판이 **간판 패널 위에 겹친다** (간판 y 1.45~1.79, 조작판 0.68~1.68) | `props.vendor` |
| **3** | **면이 정확히 같은 평면 — z-파이팅 3곳.** 자판기 간판·창틀·조작판의 뒷면이 본체 앞면(`v=0.8`)과 일치 · 브라운관 화면과 베젤 앞면이 둘 다 `z+0.130` · 통풍 슬롯이 몸통 윗면과 1mm 차 | `props.vendor` `props.terminal` |
| 4 | `terminal()` 의 `dir` 이 z 에만 걸린다. 버튼·전원등·숫자판·마우스는 `x + 상수` — **동작하지 않는 매개변수** | `props.terminal` |
| 5 | `tally.vending` 이 실제 개수와 무관하게 항상 +4. 벤치 0개를 잡아 준 게 이 tally 다 | `props.cafeteria` |
| 6 | 서버실 급기 덕트가 통로가 아니라 **랙 열 위**에 있다 (주석과 코드가 다르다) | `props.serverRoom` |
| 7 | 골판지 라벨이 `inward` 를 안 봐서 절반이 벽을 본다 | `props.carton` |
| 8 | `crac()` 드레인 배관이 함체 속이라 안 보인다 · `panel()` 에 죽은 삼항 `? 'y' : 'y'` · `props.js` 의 `W` import 미사용 · `chair()` 안의 `tube` 가 최상단 `tube` 를 다른 시그니처로 가림 · `checkInterior` 매개변수 이름이 아직 `lightTally` | 여럿 |
| 9 | 굽기 배광에 단차 — `down ≤ 0` 이면 0, `+ε` 이면 0.32. 등과 같은 높이를 지나는 물건에 딱딱한 선 | `bake.js` |

### 5.2 아직 안 만든 것

| | |
|---|---|
| **176㎡ 짜리 방 넷** | 북·남 띠가 각각 23.1 x 7.6 한 방이다. 사무실은 그렇다 쳐도 **그 크기의 설비실은 격납고**다. `CELLS` 를 쪼개고 그 사이에 복도를 넣어야 한다 — 이 평면의 제일 큰 미완성 |
| 문짝 | 지금은 구멍만 있다. 열린 문 · 닫힌 문 · 유리문 |
| 트램 승강장 | 플라자 남쪽 지선 끝이 아직 막힌 방이다 |
| 파손 · 먼지 | 지금은 너무 깨끗하다. 레퍼런스는 **사고 뒤의** 시설이다 |
| 라이트맵 검토 | 정점은 1.2m 격자가 한계다. 문 옆 그림자·소품이 만드는 그늘은 못 담는다. UV 두 번째 채널의 값어치는 유니티에서 판단한다 |
| 노말맵이 죽는 것 | 구운 빛은 정점 단위라 픽셀 단위 노말을 모른다. 반구광 0.08 과 환경맵이 조금 살릴 뿐이다 |

---

## 6. 가설 검증

이 판의 가설은 **"되면 나머지 섹터는 같은 생성기에 파라미터만 바꿔 나온다"**
였다. **아직 안 해 봤으므로 미검증이다.**

지금 구조상 새 섹터에 고칠 곳은 `layout.CELLS` 와 `PAIR`, 그리고
`props.BY_USE` 의 용도 몇 개다. 껍데기 · 조명 · 굽기 · 검사는 그대로 쓴다.
5.2 의 "176㎡ 방 넷" 을 쪼개는 작업이 그대로 **첫 검증**이 된다 — `CELLS` 만
고쳐서 되는지 보면 된다.
